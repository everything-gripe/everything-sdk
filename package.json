{
  "name": "everything-sdk",
  "version": "0.7.28",
  "description": "Simplify integration with the Everything middleware using the Everything SDK. Streamline development and enhance app capabilities with defining types for Groups, Users, Posts, and Comments.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc",
    "publish": "tsc && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "everything-sdk"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.1.3"
  },
  "files": [
    "lib"
  ],
  "dependencies": {
    "@types/showdown": "^2.0.1",
    "open-graph-scraper": "^6.2.1",
    "showdown": "^2.1.0"
  }
}
